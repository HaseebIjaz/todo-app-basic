import React from 'react'

type Props = {}

const AddNewItem = (props: Props) => {
    return (
        <div>
            <input type="text" />
            <button>Add</button>
        </div>
    )
}

export default AddNewItem